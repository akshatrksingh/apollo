# changelog.md

Intentional deviations from the design docs only. Not a git log. Claude Code appends a row here when the human approves a change to the plan (schema, signatures, task design, stage order). Each entry must cite which doc/section it changes and why.

| date | stage | change | reason | approved by |
|---|---|---|---|---|
| 2026-06-10 | 5 | post-only expected score corrected 0.33 -> 0.67 in tasks_spec.md exploit table (Task 3 row "post to C002 but never touch any task") and dev_stages.md Stage 5 test goal | clean single C002 post consumes the allowed message budget, so it is not collateral damage under the path-agnostic AllowedChanges schema; symmetric to update-no-post (each fails exactly one assertion) | Akshat |
| 2026-06-10 | 6 | transcript layout changed from flat transcripts/<task>_<model>_trial<N>_<UTC>.json to one subfolder per run transcripts/<task>_<model>_<UTC+micros>/trial<N>.json (dev_stages.md Stage 6 build item names the flat pattern) | a run writes N trial transcripts; a per-run subfolder groups them and the microsecond run stamp removes the residual cross-run same-second filename collision risk in the flat scheme | Akshat |
| 2026-06-10 | 6 | verify() in tasks/primary_xservice.py and tasks/baseline_slack.py converted to binary: return 1.0 iff ALL assertions pass, else 0.0; verifier_design.md Partial credit section described equal-weighted mean; tests asserting old 0.33/0.67 values updated to 0.0 | partial-credit scores contaminated by empty/mechanical trials make the headline metric uninterpretable; binary separates "fully solved" from "partially solved" and matches the loss-analysis use case; per-assertion booleans are preserved in every transcript for diagnostic detail | Akshat |
| 2026-06-10 | 6 | failure_stage categories changed from {no_tool_call, stalled_after_search, wrong_task, reached_post_no_task, success, other} (proposed in session spec) to {no_tool_call, success, read_only, write_errors_only, partial_writes, other} | original categories are primary_xservice-specific and do not cleanly partition both tasks; wrong_task requires knowing ALLOWED_CHANGES target which is not in the trajectory; new categories are computable from trajectory tool names + result.ok + score with no task-specific knowledge; per-assertion booleans carry the task-specific "what failed" detail | Akshat |
| 2026-06-10 | 6 | stop_reason is loop-mechanic only: {natural_stop, no_tool_call, max_turns, api_error}; success removed as a stop_reason value | "success" requires post-hoc score computation; the loop cannot determine success at stop time; loop-mechanic values are unambiguous and settable during the loop; success is read from the score field directly | Akshat |
| 2026-06-10 | 6 | aggregate field pass_k renamed to success_rate; pass_at_k = 1-(1-success_rate)^trials and pass_all_k = success_rate^trials added; printed summary now labels all three correctly | the old pass_k was the empirical per-trial success rate, mislabeled as pass^k in the print output; new field names match standard RL eval conventions; old naming would make pass@k and pass^k indistinguishable in the report | Akshat |
| 2026-06-10 | bug-fixes | BUG-04: T003 title changed to "Stabilize the PYMT-SVC component", description to "PYMT-SVC dependency graph has a fault. Identify the degraded node and mitigate." No keyword from M007 text or the instruction appears in the new title/description, so search_tasks(query=...) cannot find T003; the agent must use search_tasks(slack_message_id="M007"). Fourth assertion cross_service_link_used added: search_tasks(slack_message_id="M007") must appear in trajectory before the first update_task call. Honest desk-check: 1.0. Shortcut desk-check (update T003 + post, no link traversal): 0.0. tasks_spec.md and verifier_design.md updated. | tasks/primary_xservice.py measured keyword-matching not cross-service reasoning; the shortcut was indistinguishable from the honest path in the score signal | Akshat |
| 2026-06-10 | bug-fixes | BUG-02: task_status_correct (a1 in primary_xservice) changed from presence-based (state_after T003.status == "in_progress") to change-based (state_before T003.status == "todo" AND state_after T003.status == "in_progress") | anchors expected initial state; makes verifier self-documenting and protects against seeds that pre-set T003 to in_progress | Akshat |
| 2026-06-10 | bug-fixes | BUG-05: add_reaction in env/slack.py now normalizes emoji by stripping surrounding colons before storage (emoji.strip(":")); baseline assertion updated to check "eyes" key instead of ":eyes:"; add_reaction schema description documents both forms are accepted | a correct :eyes: reaction scored 0 if the agent passed "eyes" without colons, artificially depressing baseline scores | Akshat |
| 2026-06-10 | bug-fixes | BUG-03: target_reacted (a1 in baseline_slack) changed from presence-based to change-based: current_user must be absent from M026.reactions["eyes"] in state_before AND present in state_after | same class of bug as BUG-02; reaction present at episode start would not be agent-caused | Akshat |
| 2026-06-10 | bug-fixes | BUG-01/BUG-08: harbor/adapter.py _wrap method now passes copy.deepcopy(result) to logger.log instead of the live result reference; live result is still returned to the caller | trajectory logger stored live dict references; subsequent mutations retroactively changed earlier logged results, making serialized transcripts show final state instead of call-time state | Akshat |
| 2026-06-10 | bug-fixes | BUG-07: ApolloHarborTask.static_tool_schemas() static method added to harbor/adapter.py; run_trials() now calls it instead of instantiating a full seeded harbor task solely for schema extraction | run_trials() was seeding a workspace per invocation just to read constant schema dicts, wasting one full seed per run | Akshat |
| 2026-06-10 | bug-fixes | BUG-06: no code change; pre-binary runs (scores 0.333) are incompatible with current binary scoring and must be excluded from any aggregate or sweep comparison; runs taken before the 2026-06-10 binary-scoring changelog entry are excluded from comparisons | old proportional scoring produced fractional scores not in {0.0, 1.0}; mixing with binary runs makes headline metrics uninterpretable | Akshat |
| 2026-06-10 | new task | vendor_reconcile promoted from tasks/_experimental/exp_vendor_reconcile_short.py to tasks/vendor_reconcile.py (TASK_ID renamed from exp_vendor_reconcile_short to vendor_reconcile); seeds on rich_workspace() rather than base_workspace(); 5 assertions, binary AND-scored; Gemma 4 26B measured success_rate ~0.20 at promotion; tasks_spec.md updated with Task 4 entry | hard cross-world assembly task targeting ~0.2-0.4 success band; world difficulty from scattered amounts, one correction, missing-data trap, two-Sams disambiguation, two-Bobs disambiguation | Akshat |
